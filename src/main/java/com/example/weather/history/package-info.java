/**
 * Module: history — persists search history to a JSON file.
 *
 * <p>Appends an entry per successful lookup and reads them back for the
 * {@code history} command.
 */
package com.example.weather.history;
