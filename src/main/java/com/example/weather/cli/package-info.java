/**
 * Module: cli — command surface and formatted terminal output.
 *
 * <p>Parses arguments, dispatches the weather / forecast / history / demo
 * commands, and renders aligned, readable output.
 */
package com.example.weather.cli;
